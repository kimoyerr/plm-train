# Setup to be used to install the package

from setuptools import setup, find_packages

setup(
    name='plm-train',
    version='0.1',
    description='Protein Language training package',
    author='Krishna Yerramsetty',
    author_email='kimoyerr@gmail.com',
    url='http://example.com/your-package-name/',
    packages=find_packages(),
)
